"""Unit tests. Discover them with:

    python3 -m unittest discover -s tests -t .

Individual modules still run by name, for example:

    python3 -m unittest tests.test_claim_ledger

Discovery runs every module, including the speech sound pattern tests that
rebuild committed reports from the private research data. Those take many
minutes. Run a single module by name when you want a fast check.
"""
